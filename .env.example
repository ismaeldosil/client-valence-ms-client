# ===========================================
# Teams Agent Integration - Environment Config
# ===========================================

# Environment
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# Mock Servers (Phase 0)
MOCK_AGENT_PORT=8080
MOCK_WEBHOOK_PORT=3000

# Teams (Optional for Phase 0)
# Get this from Teams Admin > Connectors > Incoming Webhook
TEAMS_INCOMING_WEBHOOK=

# Phase 1: Notificaciones (Agent -> Teams)
# For local testing, use mock webhook: http://localhost:3000/webhook
# For production, use real Teams Incoming Webhook URLs
TEAMS_WEBHOOK_ALERTS=http://localhost:3000/webhook
TEAMS_WEBHOOK_REPORTS=http://localhost:3000/webhook
TEAMS_WEBHOOK_GENERAL=http://localhost:3000/webhook
NOTIFIER_API_KEY=dev-api-key
NOTIFIER_PORT=8001

# Phase 2: Consultas (Teams -> Agent)
# TEAMS_WEBHOOK_SECRET=
# RECEIVER_PORT=3000
# AGENT_PROTOCOL=rest
# AGENT_BASE_URL=https://your-agent.aws.com
# AGENT_API_KEY=
# AGENT_TIMEOUT=30

# Phase 3: Memoria
# SESSION_STORE=redis
# SESSION_TTL_HOURS=24
# SESSION_MAX_MESSAGES=50
# REDIS_URL=redis://localhost:6379/0
